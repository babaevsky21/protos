version: '3'

tasks:
  generate:
    desc: "generate protobuf code"
    cmds:
      - protoc -I ./protos --go_out=./protos/gen --go-grpc_out=./protos/gen --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative ./protos/proto/login.proto
      - protoc -I ./protos --go_out=./protos/gen --go-grpc_out=./protos/gen --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative ./protos/proto/feeds.proto